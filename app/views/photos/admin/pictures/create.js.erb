<% if @image.new_record? %>
alert("Failed to upload image: <%= j @image.errors.full_messages.join(', ').html_safe %>");
<% else %>
$("#images").html("<%= j render partial: 'photos/admin/pictures/picture', collection: @gallery_images if @gallery_images.present? %>");
<% end %>
